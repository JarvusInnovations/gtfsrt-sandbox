version: 2

models:
  - name: base_service_alerts
    description: Materialize data from parquet files in the DuckDB database.
  - name: base_vehicle_positions
    description: Materialize data from parquet files in the DuckDB database.
  - name: base_trip_updates
    description: Materialize data from parquet files in the DuckDB database.